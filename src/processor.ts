const processRecord = (record: string[], headers: string[]): void => {
  // TODO
  // eslint-disable-next-line no-console
  console.log('>>processRecord', { record, headers });
};

export { processRecord };
